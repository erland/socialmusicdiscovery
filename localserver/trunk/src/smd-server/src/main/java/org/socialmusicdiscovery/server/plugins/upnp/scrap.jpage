String[] opts = {"Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"};SMDApplication.main( opts );List cannot be resolved to a typeVirtualMachineDescriptor cannot be resolved to a typeVirtualMachine cannot be resolved to a typeVirtualMachine cannot be resolvedType mismatch: cannot convert from element type Object to VirtualMachineDescriptorString name = "org.socialmusicdiscovery.server.business.logic.SMDApplication";List<VirtualMachineDescriptor> vms = VirtualMachine.list();for (VirtualMachineDescriptor vmd: vms) {    if (vmd.displayName().equals(name)) {        VirtualMachine vm = VirtualMachine.attach(vmd.id());//        String agent = ...//        vm.loadAgent(agent);    }    else {    	System.err.println("skipping vm: "+vmd.displayName());    }}