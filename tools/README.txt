Create a separate directory with a structure as follows for each new tool. Tools are only small utilities that isn't an integrated parts of "localserver" or "plugin" projects.

sometool
    trunk
    branches
    tags

